## Quickstart

``` sh
# Clone the repo
git clone git@github.com:virgoone/rollup-starter-kit.git

# Move into the repo
cd rollup-starter-kit/

# Install dependencies
npm install

# Start the watcher
npm run watch
```
